import React from 'react'
import styled from 'styled-components'

const Portfolio = (props) => (
  <section id="portfolio">
    <H1><i class="fa fa-folder-open-o" aria-hidden="true"></i> Portfolio</H1>
    <Hr id="divider" />
  </section>
)

const H1 = styled.h1`
`
const Hr = styled.hr`
`

export default Portfolio
